# Parça Avcısı — MVP Yol Haritası

## DURUM — 14 Ağustos 2026
- [x] Ana sayfa, marka dili ve responsive temel
- [x] Arama, kategori keşfi, demo ilanlar ve güvenli fallback
- [x] Supabase istemcisi, auth/favori/ilan servis temeli
- [x] GitHub Actions uzak doğrulaması: `npm install` ve `npm run build`
- [x] Hiyerarşik manuel araç seçici: tip → marka → model → kasa/nesil → yıl → motor
- [x] Araç katalog sağlayıcı/adaptör altyapısı; VIN çözümleme kasıtlı olarak kapalı
- [x] Üyelik gerektiren ilan verme ve ilan detay akışları
- [x] İlan önizleme ve taslak oluşturma akışı
- [x] Taslak sonrası ilan detayına yönlendirme; İlanlarım (kullanıcının kendi ilanları) ve durum rozeti
- [x] Fotoğraftan/toplu ilan taslağı için provider tabanlı altyapı
- [x] AI tahminleri için kullanıcı kontrolü ve düşük güven uyarısı
- [x] GitHub Actions uzak build doğrulaması (run #28, #37)
- [x] Ücretsiz Tesseract.js OCR ve tarayıcı barkod/QR algılama
- [x] OCR/katalog güven skoru ve güvenli fallback modu
- [x] Araç verisi kaynak/lisans dokümantasyonu ve Supabase katalog migrasyonu
- [x] Ayrı giriş/kayıt formları, e-posta doğrulama akışı, şifre sıfırlama, oturum durumu ve Hesabım
- [x] Hesap merkezi: profil, ilan yönetimi (yayınla/durdur/satıldı/düzenle/sil), favoriler, kayıtlı aramalar, bildirimler, hesap bilgileri, ayarlar, yardım ve çıkış
- [x] Satıcı ile mesajlaşma (konuşma listesi, mesajlaşma penceresi, okundu işaretleme)
- [x] Bildirim altyapısı: trigger'lar, header çanı + okunmamış rozet, kayıtlı arama eşleştirme
- [x] Migration `20260814_messages_notifications.sql`: notifications tablosu, profile settings, saved_searches.notify, RLS genişletmesi
- [x] Migration `20260815_listing_photos_storage.sql`: `listing-images` storage bucket + storage.objects RLS, `listing_images` RLS (public okuma, satıcı yönetimi) + `is_cover` kolonu
- [x] İlan fotoğrafları: Supabase Storage upload → `listing_images` kaydı → kapak görseli kartlarda → detay galerisi (thumbnail + bozuk görsel fallback)
- [x] Birleşik ilan listesi/arama (`src/lib/listing-view.js`): gerçek ilanlar tek grid, anlık yenileme (yayın/sil sonrası `parca:listings-updated`), başlık/parça/araç/OEM/kategori/açıklama araması, en yeni önce, duplicate yok, Sıfır/2. El/Çıkma filtreleri (0 KM → "Sıfır")
- [x] Araç kataloğu genişletme (`vehicle-catalog.js`): Tofaş/Ford/Renault/Fiat/VW/Opel/Peugeot/Citroën/Toyota + BMW/Audi/Mercedes/Honda/Hyundai/Kia/Škoda/Nissan/Suzuki; Marka → Model → Yıl → Versiyon sırası; eski model yılları (Şahin 1989–1999, Escort 1981–2000 vb.)
- [x] Migration `20260815_vehicle_catalog_expansion.sql`: `vehicle_types`/`vehicle_makes`/`vehicle_models` idempotent seed (on conflict do nothing, mevcut veri bozulmaz)
- [x] İlan oluşturma/düzenleme akışı (profesyonel pazaryeri): kategori + alt kategori menüsü (arama filtresi, `src/lib/part-categories.js`), araç bilgisinin ilana kaydedilip detayda gösterilmesi, İlan Ver'de "İlanı Yayınla" (aktif, F5'siz listeye düşer) + "Taslak Kaydet" seçenekleri, kapsamlı önizleme (fotoğraf/kapak/araç/kategori/fiyat/şehir/açıklama), Hesabım → İlanlarım → Düzenle'de fotoğraf yönetimi (mevcut thumbnail + kapak rozeti, sil/taşı/kapak yap, çoklu yeni ekleme, `is_cover`/`sort_order` Supabase senkronu)
- [x] Migration `20260815_listing_metadata.sql`: `listings` tablosuna `category`/`subcategory`/`vehicle` kolonları + `listings_category_idx` (canlı DB'de doğrulandı: `listings.category`/`stock_count` mevcut)
- [x] Gerçek ilan detay sayfası (`src/lib/listing-detail.js/.css`): modal kaldırıldı, "İncele" artık `#/ilan/<id>` hash rotasına gider; sayfa yenilenince Supabase'den doğrudan yüklenir; geri butonu; büyük kapak + thumbnail galerisi (tıkla/oku ile gezinme, fotoğraf yoksa placeholder); ilan bilgileri (başlık/fiyat/durum/marka/model/yıl/versiyon/kategori/alt kategori/parça adı/OEM/şehir/ilan tarihi/ilan no/açıklama/araç uyumluluğu); satıcı kartı (profil, bireysel/kurumsal, şehir, aktif ilan sayısı, üyelik tarihi, iletişim); satıcının diğer ilanları; favori + paylaş; masaüstünde solda galeri/sağda özet, mobilde tek kolon (siyah/altın premium tasarım)
- [x] `npm run lint` betiği eklendi (`scripts/lint.mjs`, bağımlılıksız sözdizimi kontrolü)
- [x] İlan detayı profesyonelleştirme: breadcrumb (← İlanlar > Kategori > Alt Kategori > Parça, tıklanabilir, mobilde yatay kaydırma); satıcı kartına telefon gösterimi + "Telefonla Ara" + WhatsApp (settings.whatsapp veya mobil numaradan türetme) + "Mesaj Gönder"; mobilde sabit iletişim çubuğu (Ara/WhatsApp/Mesaj); Türkçe duyarlı baş harf normalizasyonu (başlık, şehir, kategori, alt kategori, marka, model, satıcı adı — büyük harf akronimler korunur, DB değişmez); eksik veride alan gizleme/"Belirtilmemiş"; `getProfileById`'ye `settings` alanı eklendi
- [x] İlan detayı veri kaybı düzeltmesi: `listing_vehicles` tablosunda RLS okuma politikası yoktu → Marka/Model/Yıl/Versiyon embed'i her zaman `[]` dönüyordu; `20260816_listing_vehicles_rls.sql` (salt-okunur) + `schema.sql` eklendi. `part:parts(name, category, subcategory, oem_number)` embed'i genişletildi; `toListingCard`/`getListingById`'de part kaynaklı alt kategori/OEM/kategori fallback'leri eklendi; detayda 16 zorunlu alanın tamamı artık "Belirtilmemiş" fallback'iyle her zaman render ediliyor; "Araç uyumluluğu" kutusu yapısal araç verisinden türetilen etiketle her zaman gösteriliyor
- [x] Marka/brand varlıkları: `parca-avcisi-logo-seffaf.png` (1337x760) içindeki piston + dişli P amblemi kırpılarak (yeniden çizilmedi, oran korundu) `public/logo-emblem.png` (ham), `public/logo-emblem-badge.png` (altın yuvarlak köşeli rozet), `public/favicon.png` (64), `public/icons/icon-192.png`, `public/icons/icon-512.png` (+maskable), `public/icons/apple-touch-icon.png` (180) üretildi. `public/manifest.json` oluşturuldu; `index.html`'e favicon + apple-touch-icon + manifest bağlantıları eklendi; `app.js` header/footer brand'i imgeye taşındı; `styles.css` `.brand-mark` imge stili + mobil kompakt (26px) boyut. Orijinal logo dosyası korunuyor. Build 95 modül ✓, lint 21 dosya ✓

## DURUM — 15 Ağustos 2026 (Faz 1-9: "Parça Arıyorum" eşleştirme sistemi)
- [x] Migration `20260816_part_requests.sql`: `part_requests` (alıcı talep tablosu; vehicle_make/model/year/version, part_category, part_name, oem_number, description, city, condition new/used/salvage/any, delivery, status active/answered/closed), `part_request_responses` (satıcı "Bende Var"; `unique(request_id, seller_id)` çift cevabı engeller), `part_request_images` (opsiyonel talep fotoğrafları), `listings.delivery`, `messages.request_id` + `listing_id` nullable (talep konuşmaları), `notifications` type check'e `request_response`/`part_request` + `related_request_id`, index'ler, tam RLS (part_requests: owner + public açık talepler okur; responses: owner veya cevap veren satıcı; images: açık taleplerde public okuma, owner yönetir; `profiles`: talep tarafları birbirini okur), trigger'lar `notify_request_response` (cevap → talebi "answered" yapar + alıcıya bildirim) ve `notify_new_message` güncellemesi (request_id'li mesajlarda erken return, çift bildirimi önler). `supabase/schema.sql` senkronlandı. **Canlı DB'ye henüz uygulanmadı.**
- [x] `src/lib/delivery.js`: `DELIVERY_OPTIONS` + `deliveryLabel` (kargo/ambar/elden/teslimat)
- [x] `src/lib/part-requests.js`: servis katmanı — `getMyPartRequests`, `getActivePartRequests(filters)` (marka/model/yıl/kategori/şehir), `getPartRequestById`, `createPartRequest`, `updatePartRequest`, `setPartRequestStatus`, `respondToRequest` (response insert + `sendMessage(requestId)` non-fatal + duplicate hatası Türkçe mesaja çevrilir), `attachRequestImages` (upload + rollback), `deleteRequestImage`; `REQUEST_CONDITION_LABELS`/`REQUEST_STATUS_LABELS`, request select embed'leri (owner, images, responses+seller)
- [x] `src/lib/messages.js`: request desteği — `getMyMessages`'a `request:part_requests(...)` embed, `sendMessage({listingId, requestId, receiverId, body})` (ikisi de opsiyonel), `markConversationRead` request_id ile
- [x] `src/lib/part-requests-ui.js/.css`: talep formu modalı (marka/model datalist + canlı model önerisi, kategori select + arama, condition "Farketmez/Sıfır/2. El/Çıkma", teslimat select, opsiyonel fotoğraf, ilk fotoğraf kapak; create/edit ayrımı + foto silme/yükleme), hash rotası `#/talep/<id>` detay sayfası (galeri, bilgi kartı, durum rozeti; alıcıya gelen cevaplar + "Mesajı Aç", satıcıya BENDE VAR / "✓ cevap verdin"), `window.__openRequestForm`/`__openRequestDetail`, `prefillFromSearch` (sorgudan marka/model/yıl/kategori/parça çıkarımı), `[data-open-request]`/`[data-request-empty]`/`[data-open-request-detail]` delegasyonu
- [x] Akıllı arama (`listing-view.js`): token bazlı AND eşleşme, `toLocaleLowerCase('tr-TR')`, yıl token'ları için doğrudan + "1989–1999" yıl aralığı eşleşmesi, alan ağırlıklı relevance skoru (title > partName/kategori/araç/OEM > açıklama/şehir/satıcı) ve skor sıralaması; boş sonuç ekranı "Parçayı bulamadın mı? Talep oluştur" → sorgu form'a ön doldurulur
- [x] Ana sayfa CTA bölümü (`app.js` + `part-requests.css`): "🔎 Parça Arıyorum" (talep formu) / "🔧 Parça Satıyorum" (ilan formu) marketplace kartları
- [x] İlan akışı teslimat tercihi (`ui-flows.js`, `listings.js`, `listing-detail.js`): ilan formuna teslimat select'i, önizlemede gösterim, `createListing`/`updateListing`/`listingSelect`'e `delivery`, detay sayfasında "Teslimat" satırı
- [x] Hesap merkezi (`account-center.js`): "Taleplerim" (buyer: durum sekmeleri, İncele/Düzenle/Kapat/Tekrar Aktif Et), "Müşterilerin Aradığı Parçalar" (seller: marka/model/yıl/kategori/şehir filtreleri + BENDE VAR + "✓ Cevap verildi"), request-tabanlı konuşmalar (Mesajlarım'da talep başlığı + "Talebi gör", chat formu request_id ile gönderir), `window.__openRequestThread`, bildirimlerde `related_request_id` → "Talebi gör"; `parca:requests-updated` ile panel yenileme
- [x] Doğrulama: `npm run lint` ✓ (24 dosya), `npm run build` ✓ (99 modül)

## DURUM — 15 Ağustos 2026 (Faz 10-15: canlı DB doğrulaması, talep bildirimi, realtime, ana sayfa girişi)
- [x] Faz 10 — Canlı Supabase durum incelemesi (anon key + PostgREST, salt-okunur). Proje `jhlrhmzxcdubsbgkbecx.supabase.co` gerçek ve erişilebilir (auth 200, REST çalışıyor). Uygulanmış migration'lar tablo/kolon probe'larıyla doğrulandı: `20260814_auth_listings_schema` (profiles=1, listings=2, listing_images=3, favorites/saved_searches/messages/listing_vehicles var), `20260814_messages_notifications` (profiles.settings, notifications, saved_searches.notify), `20260814_vehicle_catalog` + `20260815_vehicle_catalog_expansion` (vehicle_types=10, makes=18, models=99), `20260815_listing_metadata`, `20260815_listing_photos_storage` (bucket + storage RLS canlıda çalışıyor: public okuma ✓, klasör=kullanıcı UUID). Uygulanmamış (KESİN): `20260816_part_requests.sql` — `part_requests`/`part_request_responses`/`part_request_images` 404; `listings.delivery`, `messages.request_id`, `notifications.related_request_id` kolonları yok. Uygulanmamış (büyük olasılıkla): `20260816_listing_vehicles_rls.sql` (RLS içeriği REST ile doğrulanamaz; aynı tarih grubundaki part_requests migration'ı uygulanmadığı için kabul edildi). Kısıt: service-role key ve Supabase CLI yok → SQL uygulanamıyor, RLS politika içerikleri listelenemiyor; migration dosyaları **hazır ama canlıya uygulanmadı**.
- [x] Faz 12 — `20260817_notify_part_request_created.sql`: `notify_part_request_created` trigger'ı (yeni talep → aktif ilanı olan, kategorisi/şehri eşleşen satıcılara `part_request` bildirimi; `notify_requests` profil ayarı varsayılan açık) + `supabase_realtime` publication'ına `part_requests`/`part_request_responses` kaydı (idempotent DO blokları). `schema.sql` senkronlandı. Canlı DB'ye uygulanmadı.
- [x] Faz 13 — `src/lib/request-realtime.js` (yeni): `postgres_changes` INSERT (`part_requests`/`part_request_responses`) → `parca:requests-updated` event'i; seller/buyer panelleri polling'siz yenilenir; `supabaseConfigured` guard + try/catch; `main.js`'te başlatılır. Realtime publication migration'ı gerektirir (20260817).
- [x] Faz 14 — Ana sayfa piyasa bölümüne 3. kart: "Müşteri Taleplerini Gör" (`data-open-customer-requests`) → üyelik sonrası Hesabım → "Müşterilerin Aradığı Parçalar" (`__requireMember` + `__openAccountCenter('musteri-talepleri')`); `part-requests.css`'e tam genişlik kart stili.
- [x] Faz 15 — Doğrulama: `npm run lint` ✓ (25 dosya), `npm run build` ✓ (100 modül)

## YETKİ + ANA SAYFA MARKETPLACE — 15 Ağustos 2026
- [x] `20260818_part_request_permissions.sql` (YENİ, uygulanması ZORUNLU): part_requests/part_request_responses/part_request_images RLS politikaları idempotent biçimde kesinleştirildi; deterministik `grant`'ler; `part_requests` için yeni **sahip DELETE** politikası; responses'ta bilinçli olarak UPDATE/DELETE politikası YOK (varsayılan red) + olası gevşek politikalar drop edilir; profiles "part request parties read profiles" yeniden oluşturulur. FK'lara/trigger'lara dokunulmaz, veri silinmez.
- [x] Frontend savunma katmanı: `updatePartRequest` / `setPartRequestStatus` artık `assertPartRequestOwner` ile talebi veritabanından okuyup `user_id` eşleşmesini doğrular; yalnızca sahibi düzenleyip kapatabilir (RLS'in yanında ikinci katman).
- [x] Ana sayfa marketplace bölümü: "MÜŞTERİLERİN ARADIĞI PARÇALAR / Alıcılar bu parçaları arıyor" (`#talep-market`) — aktif talepler kamuya açık kartlar (parça adı, araç marka·model·yıl, şehir, durum rozeti Aktif/Cevap Geldi, tarih, [İncele] + [BENDE VAR]/[✓ Cevap verdin]); sahibi kendi talebinde yalnızca [İncele]; girişsiz görüntüleme açık, BENDE VAR/oluşturma üyelik ister. `requests-home.js` modülü + `app.js` bölümü + `part-requests.css` stilleri; `parca:requests-updated` ile yenilenir.
- [x] "SATILAN PARÇALAR" / "MÜŞTERİLERİN ARADIĞI PARÇALAR" iki taraf ana sayfada net ayrıştı (ilanlar başlığı "SATILAN PARÇALAR · YENİ EKLENENLER" olarak güncellendi).
- [x] Doğrulama: `npm run lint` ✓ (26 dosya), `npm run build` ✓ (101 modül). **Canlıda test için migration'ı uygula; iki hesaplı E2E senaryoları aşağıda Faz 11 ile güncellendi.**

## ACİL DÜZELTME — 15 Ağustos 2026 ("Talep bulunamadı" hatası)
- [x] Kök neden: `20260816_part_requests.sql` ve `schema.sql`, `part_requests.user_id` ve `part_request_responses.seller_id` FK'larını `public.profiles` yerine **`auth.users`**'a bağladı. Uygulamanın `owner:profiles!part_requests_user_id_fkey(...)` / `seller:profiles!part_request_responses_seller_id_fkey(...)` embed'leri (part-requests.js `requestSelect`) bu yüzden canlı DB'de HTTP 400 PGRST200 ("no matches found") döndürdü → `getPartRequestById` hata fırlattı → `openRequestDetail` catch → "Talep bulunamadı". RLS değil, FK hedefi bozuktu. INSERT (createPartRequest) ve route (`#/talep/<db id>`) doğruydu; kayıtlar canlıda mevcuttu.
- [x] `20260817_fix_part_request_fks.sql` (YENİ, uygulanması ZORUNLU): iki FK'yı guarded/idempotent/non-destructive biçimde `public.profiles`'a yeniden bağlar (yanlış hedefli kısıt bırak/yeniden aç; veri silinmez)
- [x] `schema.sql`: `part_requests.user_id` ve `part_request_responses.seller_id` FK hedefleri `public.profiles(id)` yapıldı
- [x] `src/lib/part-requests.js`: `requestSelect`'ten FK hint'leri kaldırıldı (PostgREST tek FK'dan otomatik tespit eder; isim-bağımsız)
- [x] Doğrulama: `npm run lint` ✓ (25 dosya), `npm run build` ✓ (100 modül). **Canlıda test için migration'ı uygula.**

## MARKETPLACE V2 — 15 Ağustos 2026 (arama modları, araç-tipine-duyarlı kategori sistemi, İlan Ver seçimi, carousel, Paylaş)
- [x] Kategori sistemi araç tipine bağlandı: `src/lib/part-categories.js` zenginleştirildi (alt kategoriler detaylandı: Kaporta'da Ön/Arka Tampon, sağ/sol çamurluk ve kapılar, Bagaj Kapağı, Panel vb.); yeni `src/lib/part-catalog.js` — `VEHICLE_TREES` (Kamyon, Otobüs, Motosiklet, Pickup / Kamyonet) + `getMainCategories(type)` / `getSubcategories(type, category)` / `generalSubcategories(category)`. Binek türevi (Otomobil, SUV, Panelvan, Minibüs, Ticari, Diğer) temel ağacı kullanır. Doğrulama: Kamyon → Kaporta'da **Bagaj Kapağı yok** ✓, Motosiklet'te Depo/Grenaj ✓, Otobüs ayrı ağaç ✓, Pickup'ta Kasa/Kasa Kapağı ✓ (Bagaj Kapağı yok). Eski `PART_CATEGORY_LIST`/`subcategoriesFor` API'leri korundu (regresyon yok).
- [x] `20260819_part_request_catalog.sql` (YENİ, uygulanması ZORUNLU — bu kodun devreye girmesinden ÖNCE canlı DB'ye uygulanmalı): `part_requests.vehicle_type` + `part_requests.part_subcategory` kolonları + index'ler; additive/idempotent. `schema.sql` senkronlandı.
- [x] Arama modları (`app.js` + `part-requests.css`): hero'da `🔧 PARÇA BUL` / `👤 PARÇA ARAYANI BUL` toggle (varsayılan PARÇA BUL); mod değişince placeholder + buton metni güncellenir. PARÇA BUL → ilan grid'i; PARÇA ARAYANI BUL → yeni `#arayan-bul` bölümü + `src/lib/requests-search.js`: akıllı token ayrıştırması (araç tipi/marka/model/yıl/kategori/parça adı; tr-TR duyarlı), filtre formu (araç tipi → kategori → alt kategori bağımlı select'ler, marka/model datalist, yıl, şehir, parça durumu), sonuçlar talep kartları (İncele/BENDE VAR), boş sonuç → "Bu parçayı arayan bir müşteri bulamadık." + [PARÇA ARIYORUM] (sorgu prefille), "← İlanlara Dön".
- [x] İlan Ver seçimi (`ui-flows.js` + `flows.css`): `+ İlan Ver` (`#sellBtn`/`#mobileSell`) → "Ne yapmak istiyorsun?" modalı → [🔧 Parça Satıyorum] (mevcut ilan formu) / [🔎 Parça Arıyorum] (talep formu). Marketplace "Parça Satıyorum" kartı (`[data-open-sell]`) doğrudan ilan formunu açar.
- [x] İlan formu (`ui-flows.js`): Araç Tipi alanı eklendi (Tip → Marka → Model → Yıl → Versiyon sıralı bağımlı select'ler); araç tipi değişince kategori listesi o tipin ağacından yeniden dolar, alt kategori aynı şekilde bağlı; `data.vehicle` önüne araç tipi eklenir. Talep formu (`part-requests-ui.js`): Araç Tipi + Alt Kategori alanları, aynı bağımlı akış; kayıt/arama `vehicle_type`/`part_subcategory` ile yapılır.
- [x] Ana sayfa talep carousel'i (`requests-home.js` + `part-requests.css`): >4 talep varsa 4'lü slide'lar, 4.5 sn yavaş rotator, hover/focus/touch'ta duraklar, `prefers-reduced-motion` saygısı, ok + nokta navigasyonu, az kartta statik grid; `[Hepsini Gör →]` korundu.
- [x] Paylaş (`part-requests-ui.js`): talep detay hero'suna "Paylaş" — Web Share API, yoksa clipboard kopyalama (bağlantı kopyalandı); URL yalnızca talep id'si içerir (özel veri yok).
- [x] Boş durumlar: PARÇA BUL boş sonucu artık **[PARÇA TALEBİ OLUŞTUR]** CTA (sorgu prefille); PARÇA ARAYANI BUL boş sonucu [PARÇA ARIYORUM].
- [x] Doğrulama: `npm run lint` ✓ (28 dosya), `npm run build` ✓ (103 modül); katalog ağacı Node ile test edildi.

### Faz 11 — İki hesaplı uçtan uca test senaryoları (MANUEL; `20260816` + `20260817` migration'ları canlı DB'ye uygulanana kadar çalıştırılamaz)
1. Hazırlık: `20260816_part_requests.sql`, `20260817_fix_part_request_fks.sql`, `20260817_notify_part_request_created.sql`, `20260818_part_request_permissions.sql` ve `20260819_part_request_catalog.sql`'i Supabase SQL Editor (veya `supabase db push`) ile uygula; build `dist/` canlı Vercel'e/öne çıksın. **Sıra önemli: `20260819_part_request_catalog.sql`, `requestSelect` artık `vehicle_type`/`part_subcategory` kolonlarını çektiği için kod dağıtımından ÖNCE uygulanmalı.**
2. Alıcı (A / Fatma): kayıt/giriş → ana sayfa "Parça Arıyorum" → VW Passat 2016, kategori Aydınlatma, parça "ön sağ far", şehir İstanbul, condition "2. El", 1 fotoğraf → oluştur. Taleplerim'de görünür; İncele → `#/talep/<id>` detay + galeri; Düzenle; Kapat; Tekrar Aktif Et. Ana sayfa "Alıcılar bu parçaları arıyor" bölümünde talebi görür (yalnızca [İncele], BENDE VAR yok).
3. Satıcı (B): giriş → ana sayfa "Müşteri Taleplerini Gör" → Hesabım → Müşterilerin Aradığı Parçalar; marka/model/kategori filtreleri talebi bulur; BENDE VAR → "✓ Cevap verildi", çift cevap engellenir, talep "answered" olur.
4. Bildirim: A'da çan rozeti artar, `request_response` bildirimi gelir, "Talebi gör" `#/talep/<id>` açar. B'ye de `part_request` bildirimi (20260817) gelir.
5. Mesajlaşma: Mesajlarım'da talep konuşması (`other::r<requestId>`), chat formu request_id ile gönderir, taraflar okur.
6. Realtime: B hesabı "Müşterilerin Aradığı Parçalar" paneli açıkken A yeni talep oluşturursa panel bildirim/realtime ile (polling'siz) güncellenir.
7. Arama regresyonu: "2016 Passat ön sağ far" → başlıkta passat/far geçenler önce; yalnızca açıklamada geçen ilanlar da bulunur (düşük ağırlık); boş sonuç → "Parçayı bulamadın mı?" → sorgu talep formuna prefille dolar.
8. Yetki (RLS + UI, iki hesapla): Fatma(A) talebi oluşturur. Ahmet(B): talebi ana sayfada ve detayda görebilir ✓; detayda Düzenle/Talebi Kapat **görünmez** ✓; BENDE VAR diyebilir ✓ (sonrası "✓ Bu talebe cevap verdin" + [Mesajı Aç], çift cevap engellenir) ✓; mesaj gönderebilir ✓; talebi düzenleyemez/kapatamaz/silemez ✓ (UI yok + RLS: anon PATCH 401, authenticated non-owner UPDATE RLS ile reddedilir). Fatma: kendi talebini görüntüler/düzenler/kapatır ✓, gelen cevapları görür ✓. Anonymous: aktif talebi görür ✓; Düzenle/Kapat görmez ✓; BENDE VAR'a tıklayınca giriş istenir ✓; telefon/e-posta hiçbir yüzeyde görünmez ✓. Kapalı talepler ana sayfada listelenmez ✓. Cevap gelince rozet "Cevap Geldi" olur ✓.

## 01 — Gerçek veri katmanı
- [ ] Supabase projesi oluşturulması ve migration uygulanması
- [ ] `20260816_part_requests.sql` ve `20260817_notify_part_request_created.sql` canlı DB'ye uygulanması (talep formu/detay/"Bende Var"/request mesajlaşması/bildirim/realtime bu migration'lar olmadan hata verir)
- [x] `@supabase/supabase-js`, environment şablonu ve client
- [x] Aktif ilanlar için Supabase okuma, yapılandırmasız demo fallback
- [x] Auth, favori ve taslak ilan oluşturma servisleri
- [x] Storage / ilan fotoğraflarının yüklenmesi (`src/lib/listing-images.js`, migration hazır; canlı DB'de doğrulandı: `listing-images` bucket + `listing_images.is_cover` + storage RLS çalışıyor)
- [x] `listings.category`/`subcategory`/`vehicle` kolonları (`20260815_listing_metadata.sql` hazır; canlı DB'ye uygulanmadı — uygulanmadan ilan oluşturma/düzenleme insert'i hata verir)
- [ ] Açık araç kataloğunun Supabase'e sürümlü içe aktarımı

## 02 — Akıllı parça arama
- [x] Araç tipi / marka / model / yıl / versiyon akışı (kasa/nesil adımı kaldırıldı; Marka → Model → Yıl → Versiyon)
- [x] Parça veya OEM arama giriş noktası (başlık, parça adı, araç bilgisi, OEM, kategori, açıklama)
- [x] Sıfır / 2. El / Çıkma filtresi ve en yeni önce sıralama
- [ ] Kaynak kapsamındaki motor/versiyon verisini içe aktarma
- [ ] Gerçek araç uyumluluğu sorgusu (liste/migration temeli atıldı)
- [ ] VIN resolver (bilinçli olarak sonraki aşama)

## 03 — İlan detayları
- [x] Üyelik kontrollü detay modalı
- [x] Fiyat, şehir, araç uyumluluğu ve favori başlangıcı
- [ ] Supabase Storage fotoğraf galerisi (galeri tamamlandı; canlı DB'de doğrulandı)
- [x] Gerçek satıcı profili ve mesajlaşma

## 04 — Üyelik ve ilan verme
- [x] Giriş/kayıt modalı ve üyelik kapısı
- [x] Ayrı login/kayıt formları, e-posta doğrulama, şifre sıfırlama ve Hesabım
- [x] İlan formu, önizleme ve taslak kayıt
- [ ] Kullanıcı profili, kayıtlı araçlar ve ilan yönetimi (profil ve ilan yönetimi hazır; kayıtlı araçlar sonraki adım)

## 05 — Sonraki MVP adımları
- [x] Mesajlaşma ve bildirimler (temel; moderasyon/güven sonraki aşama)
- [ ] "Parça Arıyorum" alıcı→satıcı eşleştirmesinin canlı DB testi: `20260816` + `20260817` migration'larını uygula, iki hesap ile talep → BENDE VAR → mesajlaşma akışını uçtan uca doğrula (bildirim + "answered" + RLS + realtime; senaryolar yukarıda Faz 11)
- [x] Yeni talep bildirimi: `part_request` type trigger hazır (`20260817_notify_part_request_created.sql`); canlı DB'ye uygulanmadı
- [x] Satıcılar için ana sayfa giriş noktası ("Müşteri Taleplerini Gör" kartı) + realtime abonelik (`request-realtime.js`; `supabase_realtime` publication migration'ı gerektirir)
- [ ] Moderasyon, kullanıcı güveni ve satıcı puanlaması
- [ ] Production güvenlik/performance/SEO/PWA
- [ ] Vercel ve Android yayını

## DURUM — 15 Ağustos 2026 (final denetim: iki taraflı pazaryeri + arama + kategori sistemi)
- [x] **Yetki denetimi (FAZ 2/11)**: UI + RLS birlikte doğrulandı. Talep sahibi Düzenle/Talebi Kapat/Tekrar Aktif Et'i yalnızca kendi kartında/detayında görür (`part-requests-ui.js` `renderRequestPage` → `ownerActions`, `requestResponsesHtml`); başkasında DOM'a eklenmez. App katmanı `assertPartRequestOwner` (`part-requests.js`), `respondToRequest` kendi talebini + kapalı talebi + anonymous'u reddeder; çift cevap `unique(request_id, seller_id)` + RLS (`for insert to authenticated` + `pr.user_id <> auth.uid()` + `status in ('active','answered')`). `20260818_part_request_permissions.sql` korundu.
- [x] **Arama modu kalıcılığı + metinler (FAZ 5)**: `src/app.js` — PARÇA BUL butonu "Parça Bul"/placeholder "Parça, marka, model veya parça no ara..."; PARÇA ARAYANI BUL butonu "Arayanı Bul"/placeholder "Aranan parça, araç veya şehir ara..."; mod `localStorage('pa:searchMode')` ile korunuyor, sayfa yenilemede geri geliyor.
- [x] **Boş sonuç mesajları (FAZ 10)**: Parça Bul → "Aradığın parçayı bulamadık." (`listing-view.js emptyHtml`); Parça Arayanı Bul → "Bu parçayı arayan aktif müşteri bulamadık." (`requests-search.js`).
- [x] **Alt kategori zenginleştirme (FAZ 7)**: `part-categories.js`'e spec'teki eksikler eklendi — Kaporta: Ayna, Panjur; Motor: Subap; Şanzıman: Volan, Şanzıman Kapağı, Vites Mekanizması; Aydınlatma: Ön Far, Arka Stop; Fren: ABS Sensörü; Süspansiyon: Rot, Porya; Elektrik: ECU; İç Aksam: Torpido; Jant & Lastik: Porya. (Araç tipi ayrımı `part-catalog.js` VEHICLE_TREES'te; Kamyon/Motosiklet/Otobüs/Pickup kendi ağaçlarını kullanır.)
- [x] **Carousel mobil swipe (FAZ 4)**: `requests-home.js` mountCarousel — touchstart/touchend deltaX (>40px) ile önceki/sonraki slide; 4.5 sn rotator, hover/focus duraklama, reduced-motion saygısı, ok + nokta kontrolleri korundu.
- [x] Doğrulama: `npm run lint` ✓ (28 dosya), `npm run build` ✓ (103 modül). Git commit: "Parça Avcısı iki taraflı arama ve kategori sistemi".

## DURUM — 16 Ağustos 2026 (ana sayfa sadeleştirme + kayıt/e-posta doğrulama + Müşterilerim)
- [x] **Ana sayfa sadeleştirme** (`src/app.js`): "POPÜLER KATEGORİLER / Parçayı kategoriden bul" (`#kategoriler`) ve "Parça Avcısı Nasıl Çalışır?" (`#nasil-calisir`) bölümleri kaldırıldı; sıralama hero → kartlar → Aracını Seç → Müşterilerin Aradığı Parçalar → Yeni Eklenen Parçalar → Faydalar oldu. Kategoriler nav/footer/mobil linkleri `#ilanlar`'a yönlendirildi (kategori sistemi korunur, arama yoluyla erişilir); `#categoryGrid`/`categories`/`[data-show-all]` JS'i temizlendi. "Müşteri Taleplerini Gör" kartı artık `#talep-market`'e kaydırır; "Hepsini Gör →" tüm aktif talepleri açan arayan bulma görünümünü açar (`requests-home.js`).
- [x] **Header logo boyutu** (`app.js` width 140/height 64 + `styles.css`): masaüstü `.brand-logo .brand-mark` height 64px / max-width 150px (~140px genişlik), mobil 44px / max-width 110px.
- [x] **Kayıt formu genişletme** (`ui-flows.js` + `flows.css .form-grid`): Ad + Soyad, Telefon (zorunlu, ≥10 hane doğrulama), E-posta, Şifre + Şifre (tekrar) eşleşme kontrolü, Adres (zorunlu). `signUp` (`auth.js`) telefon/adresi `user_metadata`'ya yazar; `handle_new_user` trigger'ı (migration) bunları profiles satırına kopyalar.
- [x] **E-posta doğrulama zorunluluğu** (`ui-flows.js`): kayıtta oturum yoksa doğrulama ekranı (mevcut akış) + `emailVerificationRequired` bayrağı; "E-posta doğrulaması gerekli" uyarı modalı; üyelik gerektiren işlemler (`requireMember`: ilan verme, talep oluşturma, BENDE VAR, mesaj) doğrulanmamış hesapta engellenir; "Email not confirmed" giriş hatası Türkçe uyarıya çevrilir. Güvenlik: bayrak yalnızca doğrulama gerektiren projede set edilir (doğrulama kapalı projede herkes çalışmaya devam eder).
- [x] **Profil adresi** (`profile.js` + `account-center.js`): `getMyProfile` `address` çeker, `updateProfile` `address` yazar; Profilim düzenleme formuna Adres alanı, Hesap Bilgileri'ne salt-okunur Adres gösterimi. Telefon/adres halka açık okuma politikalarına eklenmedi (gizlilik korundu).
- [x] **Müşterilerim** (`account-center.js` + `part-requests.js`): profil menüsündeki "Müşterilerin Aradığı Parçalar" → **Müşterilerim** oldu; yalnızca satıcının "Bende Var" dediği taleplerin sahipleri listelenir (`getMyRespondedRequests` — `responses:part_request_responses!inner` embed + `responses.seller_id` filtresi, RLS "part request parties read profiles" ile müşteri adı/şehri okunur). Kartta müşteri adı, parça, araç, şehir, durum, "Bende Var" tarihi, [Mesaj Aç] (`data-request-thread` → gerçek konuşma ekranı) + [Talebi Gör]. Eski filtreli pano + `requestFilters`/`requestFilterForm`/`data-pane-respond` kaldırıldı.
- [x] **Mesajlaşma canlı güncelleme** (`account-center.js` + migration): konuşmaya tıklayınca açılan gerçek mesajlaşma ekranı korundu; `messages` tablosu `supabase_realtime` publication'a eklendi ve Mesajlarım panosu/açık konuşma yeni mesajda otomatik yenilenir (`refreshMessagesPane` + `activeThreadKey`; modal kapalıyken boşta çalışmaz).
- [x] **Migration `20260820_profiles_address_verification.sql` (YENİ, uygulanması ZORUNLU — kod dağıtımından ÖNCE)**: `profiles.address` (add column if not exists), `handle_new_user` CREATE OR REPLACE (full_name + phone + address metadata kopyası), `messages` → supabase_realtime (idempotent DO block). Katmanlı; DROP/yok etme yok. **Not: `getMyProfile` artık `address` çektiği için migration uygulanmadan Profilim/Hesap Bilgileri `address` kolonu bulunamaz hatası verir.**
- [x] Doğrulama: `npm run lint` ✓ (28 dosya), `npm run build` ✓ (103 modül).

## DURUM — 16 Ağustos 2026 (ana sayfa son görsel düzeltme)
- [x] **"Müşteri Taleplerini Gör" kartı tamamen kaldırıldı** (`app.js` + `part-requests.css`): marketplace-cta bölümündeki `article.marketplace-card.browse` markup'ı, `[data-open-customer-requests]` event listener'ı ve `.marketplace-card.browse{grid-column:1/-1}` + `.marketplace-card.browse .marketplace-btn{...}` CSS kuralları silindi. Geriye iki kart (Parça Arıyorum / Parça Satıyorum) 2 sütunlu grid'de eksiksiz oturuyor; boşluk yok.
- [x] **Header logosu büyütüldü** (`app.js` width 170/height 78 + `styles.css`): masaüstü `.brand-logo .brand-mark` height 78px / max-width 190px (≈171px genişlik hissi, hedef 150–180px); `object-fit:contain` korundu, oran bozulmaz. Mobil height 52px / max-width 115px (≈114px genişlik hissi, hedef 100–115px); nav-wrap yüksekliği (masaüstü 96px / mobil 74px) değişmedi, taşma yok.
- [x] "Müşterilerin Aradığı Parçalar / Alıcılar bu parçaları arıyor" (`#talep-market`) bölümü DOKUNULMADAN korundu; "Parça Arayanı Bul" (`#arayan-bul`) gizli akış aynen duruyor. Header'da logo dışında "PARÇA AVCISI" HTML text yok (tek marka kaynağı logo dosyası).
- [x] Ana sayfa akışı: HEADER → HERO → PARÇA ARIYORUM/SATIYORUM → ARACINI SEÇ → MÜŞTERİLERİN ARADIĞI PARÇALAR → YENİ EKLENEN PARÇALAR → FAYDALAR → FOOTER.
- [x] Doğrulama: `npm run lint` ✓ (28 dosya), `npm run build` ✓ (103 modül). Dist'te "Müşteri Taleplerini Gör"/`data-open-customer-requests` yok, "MÜŞTERİLERİN ARADIĞI PARÇALAR" ve `requestMarketGrid` mevcut, CSS `height:78px` logo güncel.
- [x] **Header logosu son kez büyütüldü** (`app.js` width 230/height 105 + `styles.css`): masaüstü `.brand-logo .brand-mark` height 105px / max-width 240px (1224×558 oran ≈2.19 → ≈230px genişlik hissi, hedef 220–240px); `object-fit:contain` korundu, oran bozulmadı, kesme/e zme yok. Mobil height 62px / max-width 145px (≈136px genişlik hissi, hedef 130–145px); nav-wrap yüksekliği (masaüstü 96px / mobil 74px) değişmedi, header doğal olarak ~105px'e büyür, taşma yok. Header'da logo yanında "PARÇA AVCISI" HTML text hâlâ YOK. "Müşteri Taleplerini Gör" kartı geri gelmedi, "Müşterilerin Aradığı Parçalar" bölümü korundu.

## DURUM — 16 Ağustos 2026 (header Kategoriler dropdown)
- [x] **Header "Kategoriler" artık dropdown/mega menü** (`src/lib/categories-menu.js` + `categories-menu.css`, `app.js`, `main.js`): eski `#ilanlar` anchor davranışı kaldırıldı — masaüstü nav'daki ve mobil nav'daki Kategoriler artık `data-open-categories` butonu; `#ilanlar`'a scroll yok. `.site-header` içine `#catMenu` paneli eklendi (absolute, header altı, dark/zemin + altın vurgu, radius, shadow, `catFade` animasyonu).
- [x] Veri kaynağı: mevcut `part-categories.js` (binek ağacı) + `part-catalog.js` (`VEHICLE_TREES`: Kamyon/Otobüs/Motosiklet/Pickup) — `getMainCategories` / `getSubcategories` ile render; uydurma kategori yok. Panel başında araç tipi seçici: Otomobil (varsayılan binek) / Kamyon / Otobüs / Motosiklet / Pickup / Kamyonet; Kamyon seçilince "Bagaj Kapağı" gibi otomobile özgü alt kategoriler görünmez. İlan/arama sistemleriyle aynı ağaç kullanılır.
- [x] Etkileşim: Kategoriler'e tıkla → aç, tekrar tıkla / dışarı tıkla / ESC → kapanır. Kategori veya alt kategoriye tıklayınca menü kapanır, `window.__homeSearch` (app.js'te expose edilen mevcut `search`) ile ilgili arama çalışır ve `#ilanlar`'a kayar (arama modu mantığı korunur). Desktop'ta `auto-fill minmax(200px,1fr)` çok kolonlu; mobilde 2 kolon + max-height kaydırmalı, taşma yok.
- [x] Kaldırılan "Parçayı kategoriden bul" ana sayfa bölümü ve "SATIŞI HIZLANDIR / Müşteri Taleplerini Gör" kartı GERİ GETİRİLMEDİ; sayfa akışı değişmedi.
- [x] Doğrulama: `npm run lint` ✓ (29 dosya), `npm run build` ✓ (105 modül). Dist'te `data-open-categories`/`catMenu`/`__homeSearch`/`data-cat-type`/`data-cat-search` mevcut; header'daki eski `<a href="#ilanlar">Kategoriler</a>` yok (footer linki korunuyor).

## DURUM — 16 Ağustos 2026 (kategori menüsü gerçek ilan filtresi + ana sayfa kart küçültme)
- [x] **Kategori menüsü artık gerçek filtre** (`categories-menu.js` + `listing-view.js`): `data-cat-search` kaldırıldı; ana kategoriler `data-cat-main`, alt kategoriler `data-cat-sub` + kolon `data-cat-col` taşır. Seçim `window.__listingView.setCategoryFilter({category, subcategory, vehicleType})`'e gider (liste modundan bağımsız, deterministik; `__homeSearch` yalnızca fallback). Seçim sonrası menü kapanır ve `#ilanlar`'a kayar. Araç tipi seçici `activeType` filtrenin içine taşınır (Otomobil varsayılanı tip kısıtlaması uygulamaz — binek türevi ağaç zaten otomobil/SUV/panelvan vb. genişliğini temsil eder; Kamyon/Otobüs/Motosiklet/Pickup / Kamyonet kendi ağaçlarından gelip filtreye takılır).
- [x] **Filtre mantığı** (`listing-view.js`): `state.categoryFilter` — `matches()` önce yapısal kontrol (kategori tam eşleşme, alt kategori tam eşleşme, `matchesVehicleType`), sonra mevcut token skorlaması. `matchesVehicleType` ilanın `vehicle` metnindeki tip token'ını (gerçek ilanlarda `vehicle` "Tip · Marka · Model · ..." başlar; token bazlı, "Kamyon" ↔ "Kamyonet" karışmaz) veya `vehicleCatalog`'taki "marka model" kombinasyonunu arar. `setCategoryFilter` arama kutusunu "Kategori AltKategori" ile doldurur; `search()` ve "Tümü" (`#allListings`) filtreyi temizler; `clearCategoryFilter()` boş durumdaki [FİLTREYİ KALDIR] butonuyla çalışır.
- [x] **Boş durum**: kategori filtresi seçiliyken sonuç yoksa "**Bu kategoride henüz ilan bulunmuyor.**" + [FİLTREYİ KALDIR] + [PARÇA TALEBİ OLUŞTUR] (sorgu prefille). Genel boş mesajlar korunur.
- [x] **URL/state kalıcılığı**: filtre `?category=Kaporta&subcategory=Kaput&vehicleType=Kamyon` olarak `history.replaceState` ile adres çubuğuna yazılır (hash router'ı hiç tetiklemez, `#/ilan/<id>` detay/geri akışı bozulmaz); sayfa yenilenince `restoreCategoryFilter` query parametrelerinden filtreyi geri yükler ve arama kutusuna yazar. Manuel arama filtre parametrelerini temizler.
- [x] **Demo veri** (`demo-listings.js`): mevcut 16 ilana ağaçla birebir `subcategory` değerleri eklendi (Ön Far, Sağ Ön Kapı, Marş Motoru, Amortisör, Arka Stop, Balata, Debriyaj Seti, Ayna, Motor Komple, Ön Tampon, Akü, Far, Gösterge Paneli, Arka Tampon); Kaporta→Kaput testinin sonuç vermesi için 2 Kaput ilanı eklendi: Ford Escort Kaput (id 17, çıkma) ve VW Passat B5 Kaput (id 18, sıfır). Demo olmayan diğer alt kategorilerde boş durum mesajı istenen davranıştır.
- [x] **Ana sayfa kartları ~%20-25 küçültüldü** (`part-requests.css`): `.marketplace-cta` padding 60→44px (mobil 40→30px), kart padding 28→20px (mobil 22→18px), gap 10→7px, radius 18→14px, ikon 34→26px, başlık `clamp(20..26px)`→`clamp(17..21px)`, açıklama 13→11.5px, buton padding 13/22→10/18 + margin-top 14→10px. Fonksiyon/değer/kimlik değişmedi.
- [x] Doğrulama: `npm run lint` ✓ (29 dosya), `npm run build` ✓ (105 modül). Filtre mantığı Node testi: Kaporta→Kaput 2 sonuç (Escort+Passat Kaput), Motor 2, Motor→Motor Komple 1, Fren Sistemi 1, Fren→Balata 1, Kaporta→Kaput+Pickup tipi 0, Kamyon tipi 0 (boş durum gösterir), Süspansiyon→Amortisör 2, Aydınlatma→Ön Far 1 ✓. Dist'te `setCategoryFilter`/`data-clear-category`/"Bu kategoride henüz ilan bulunmuyor."/`data-cat-main`/`data-cat-sub` mevcut, kart CSS `padding:20px`/`gap:7px`/`border-radius:14px` doğrulandı.

## ACİL DÜZELTME — 16 Ağustos 2026 (kategori filtresi gerçek tarama bug fix)
- [x] **Kök neden**: `.env` içindeki gerçek Supabase kimlik bilgileri `supabaseConfigured = true` yaptığı için uygulama CANLI modda çalışıyordu; `demo-listings.js` yalnızca fallback olarak kullanılıyordu ve canlı DB'deki 2 ilanın ("far", "tampon") `category`/`subcategory` alanları BOŞ olduğundan HER kategori filtresi 0 sonuç döndürüyordu. Demo Kaput ilanları (id 17/18) hiçbir zaman dataset'e girmiyordu. (Canlı DB salt-okunur PostgREST sorgusuyla doğrulandı: `category` ve `subcategory` boş.)
- [x] **Demo ilanların dataset'e girmesi** (`src/lib/listing-view.js` `load()`): geliştirme ortamında (`import.meta.env.DEV`) canlı ilanların yanına `demoListings` eklenir (dedupe, ikinci sistem yok). Production build'de yalnızca gerçek ilanlar gösterilir; demo ilanlar yalnızca Supabase yapılandırılmadığında veya yüklenemediğinde devreye girer. Böylece kullanıcının `npm run dev` tarayıcısında Kaporta→Kaput iki Kaput ilanını gösterir; Vercel/üretim sahte ilan içermez.
- [x] **Boş arama kategori filtresini korur** (`listing-view.js` `search()`): boş sorgu `state.categoryFilter`'ı sıfırlamaz (setCategoryFilter sonrası yanlışlıkla yapılan boş arama filtreyi silmez); dolu manuel arama filtreyi sıfırlar. `#allListings` ("Tüm ilanları gör") artık `clearCategoryFilter()` çağırır (`src/app.js`).
- [x] **"Eski Kaput ilanı DOM'da kalıyor" şikayeti repro edilemedi**: grid `innerHTML = ...` ile tamamen yeniden yazılıyor; Kaput→Aydınlatma geçişinde eski kartlar silinir (CDP ile doğrulandı). Kullanıcı gözlemi büyük olasılıkla canlı moddaki kategorisiz veriye dayanıyordu.
- [x] **Gerçek browser doğrulaması (headless Chrome + CDP)**:
  - Dev (canlı+demo birleşik): ilk yükleme 20 ilan; Kaporta→Kaput → Ford Escort Kaput + VW Passat B5 Kaput ✓ (URL `?category=Kaporta&subcategory=Kaput`); hemen ardından Aydınlatma→Ön Far → Tofaş Şahin Sağ Ön Far (eski kartlar yok) ✓; Fren Sistemi→Balata → Fiat Egea ✓; Motor → 2 ✓; `search("")` sonrası filtre korunuyor ✓; `#allListings` → 20, filtre temiz ✓; `?category=Kaporta&subcategory=Kaput` ile yenilemede filtre geri yükleniyor ✓.
  - Demo-only (`.env` yedekli): aynı testler 18 ilanla geçti ✓.
  - Production build (`npm run build` + `vite preview`): yalnızca canlı 2 ilan ✓; kategorilerde "Bu kategoride henüz ilan bulunmuyor." + [FİLTREYİ KALDIR] + [PARÇA TALEBİ OLUŞTUR] ✓; `#allListings` → 2 ✓.
- [x] **Notlar**: `.env` içindeki anon key üretim JS'ine gömülür (Supabase anon key'leri public'tir, veri RLS ile korunur; sorun değil). Testler için `.env` geçici olarak `.env.bak` yapıldı ve geri yüklendi; test dev sunucuları (5175/5176/5177 + 4174 preview) kapatıldı, kullanıcının 5173 instance'ına dokunulmadı. **Commit/PUSH YAPILMADI** — kullanıcı onayı bekleniyor.
- [x] Doğrulama: `npm run lint` ✓ (29 dosya), `npm run build` ✓ (105 modül).

## DURUM — 16 Ağustos 2026 (ana sayfa bölüm sırası: ilanlar taleplerin üstünde)
- [x] **Bölüm sırası değişti** (`src/app.js` DOM): "Yeni eklenen parçalar" (`#ilanlar`) bölümü "Müşterilerin Aradığı Parçalar" (`#talep-market`) bölümünün **üstüne** taşındı. Yeni sıra: HEADER → HERO → PARÇA ARIYORUM/SATIYORUM → ARACINI SEÇ → YENİ EKLENEN PARÇALAR → MÜŞTERİLERİN ARADIĞI PARÇALAR → FAYDALAR → FOOTER. Yalnızca DOM/HTML bölüm sırası değişti; ID'ler (`#ilanlar`, `#listingGrid`, `#allListings`, `#talep-market`, `#requestMarketGrid`, `#allRequestsBtn`, `data-open-request` vb.), event listener'lar, filtreler, carousel, "Bende Var", arama modları ve tüm fonksiyonlar korundu.
- [x] **Görsel geçiş** (`src/styles.css`): `.listings-section`'dan `border-bottom` kaldırıldı — artık `#ilanlar`'ın altında gelen `#talep-market`'in `border-top`'u ile çift çizgi (2px) oluşmuyor; tek ince ayırıcı + `.section` doğal padding'i (74px / mobil 52px) korunuyor. Premium siyah/beyaz/altın tema değişmedi.
- [x] **Doğrulama**: `npm run lint` ✓ (29 dosya), `npm run build` ✓ (105 modül). Gerçek browser (headless Chrome + CDP, localhost:5175): bölüm sırası Hero → piyasa → aracini-sec → [arayan-bul hidden] → ilanlar → talep-market → benefits; `#ilanlar` geometrik olarak `#talep-market`'in üstünde (true); ilan kartları (20) ve talep kartları render oluyor. Test sunucusu kapatıldı. **COMMIT/PUSH YAPILMADI** — kullanıcı localhost'ta kontrol edecek.
