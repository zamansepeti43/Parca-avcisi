name: Catalog agent task

on:
  workflow_dispatch:

permissions:
  contents: write
  issues: write
  pull-requests: write

jobs:
  instructions:
    runs-on: ubuntu-latest
    steps:
      - run: echo "See .github/CATALOG_AGENT_TASK.md and CATALOG_PIPELINE_PLAN.md"
